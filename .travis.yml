language: c

install: true

branches:
  only:
    - master
    - dev

jobs:
  include:
    - stage: build
      script: make clean all
    - stage: test
      script:
          - make clean all
          - make -C tests/ all
          - tests/test-libtrie
      
